spring:
  application.name: kpi-service
  cloud:
    zookeeper:
      connect-string: localhost:2181
      discovery:
        enabled: true

server.port: ${random.int[21000,22000]}

elasticsearch:
  url: localhost
  port: 9300
  index: aoc_aop
